Text Processing Bonus
=====================


Instructions
------------
 - `virtualenv -p $(which python3) env && . ./env/bin/activate`
 - `pip install -r requirements.txt`
 - `./generate-report.py`
 - The report will be generated in the current directory with a filename "hostile-ips-top-100.XXX.csv"
